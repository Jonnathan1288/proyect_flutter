import 'package:flutter/material.dart';

class ChartPages extends StatelessWidget {
  const ChartPages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pages of of charts'),
    );
  }
}
